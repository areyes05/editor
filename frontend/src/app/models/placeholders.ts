import {Post} from './post';
import {User} from './user';

export const postPlaceholder: Post = {content: '', ownerName: '', date: new Date()};
export const userPlaceholder: User = {id: '', name: '', posts: []};
